#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://eu1.prisma.sh/megan-gilpin-40134b/shopaholic/dve
datamodel: datamodel.graphql
# secret: ${evn:PRISMA_SECRET}
hooks:
  post-deploy:
      - graphql get-schema -p prisma